{
  // Cloudflare Workers가 Next.js의 정적 빌드 결과물(out)을 웹사이트로 배포하도록 설정합니다.
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "hub",
  "compatibility_date": "2026-09-08",
  "assets": {
    // `npm run build`가 생성하는 정적 사이트 폴더를 Cloudflare에 올립니다.
    "directory": "./out"
  }
}
